import React from 'react'

function Admin() {
  return (
      //Create a reusable component for each one then use map for each one
    <div className='admin--container'>
        Admin
    </div>
  )
}

export default Admin